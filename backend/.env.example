APP_ENV=dev

# Default targets the docker-compose Postgres (`docker compose up -d` from repo root).
# Swap for your Neon connection string in production.
DATABASE_URL=postgres://cinesound:cinesound@localhost:15432/cinesound

TMDB_API_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

GEMINI_API_KEY=
GROQ_API_KEY=

GOOGLE_CLIENT_ID=

DAILY_QUERY_CAP=500

CORS_ORIGINS=["http://localhost:3000"]
